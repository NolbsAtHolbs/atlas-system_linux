#include "_getline.h"

/**
 * _getline - reads a line from a file descriptor
 * @fd: a file descriptor
 * Return: null terminated string or NULL if error || no line
*/

char *_getline(const int fd)
{
	
}
