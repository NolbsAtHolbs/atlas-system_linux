#include "multithreading.h"

/**
 * blur_image - blurs the entirity of an image using a Gaussian Blur
 * @img_blur: holds the address at which the blurred image is stored
 * @img: points to the source image
 * @kernel: points to the convolution kernel to use
 */
void blur_image(img_t *img_blur, img_t const *img, kernel_t const *kernel);
{
	
}
